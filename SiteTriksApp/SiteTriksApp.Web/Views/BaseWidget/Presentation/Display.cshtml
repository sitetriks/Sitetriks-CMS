@model SiteTriks.Extentions.WidgetModels.PresentationWidgetModel

@if (Model.IsPreview)
{
    <div class="preview-placeholder" data-identifier="@Model.Id">
        @await Html.PartialAsync("~/Areas/SiteTriks/Views/Shared/WidgetControl.cshtml", model: Model)

        <div class="@Model.CssClass">
            @await Component.InvokeAsync("PresentationWidget", new { area = "", element = Model.Element, isPreview = true })
        </div>
    </div>
}
else
{
    if (Model.CanViewByRole && Model.CanViewByGroup)
    {
        <div class="navigation-widget @Model.CssClass">
            @await Component.InvokeAsync("PresentationWidget", new { area = "", element = Model.Element, isPreview = false })
        </div>
    }
}