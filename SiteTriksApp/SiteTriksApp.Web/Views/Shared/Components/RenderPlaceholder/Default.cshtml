@model SiteTriks.Extentions.WidgetModels.MultyModel

@foreach (var widget in Model.Models)
{
    @*@await Component.InvokeAsync("RenderWidget", new { content = item, preview = (bool)ViewBag.IsPreview ? "preview" : "", lang = (string)ViewBag.Languag })*@
    @Html.Partial($"/Views/BaseWidget/{widget.Key}/Display.cshtml", widget.Value)
}
