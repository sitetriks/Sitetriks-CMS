@model SiteTriks.Models.CoreWidgets.FileResourceWidgetViewModel

@if (Model.IsRaw)
{
    <style data-style="head-bot">
        @Html.Raw(Model.RawCode)
    </style>
}
else
{
    <link data-style="head-bot" rel="stylesheet" type="text/css" href="@Model.Url">
}
