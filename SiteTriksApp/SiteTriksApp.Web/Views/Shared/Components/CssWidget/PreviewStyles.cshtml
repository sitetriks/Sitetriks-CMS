@model SiteTriks.Models.CoreWidgets.FireResourceWidgetViewModel

<div>
    @if (Model.IsRaw)
    {
        @Model.RawCode
    }
    else
    {
        <b>External css resource from @Model.Url</b>
    }
</div>
