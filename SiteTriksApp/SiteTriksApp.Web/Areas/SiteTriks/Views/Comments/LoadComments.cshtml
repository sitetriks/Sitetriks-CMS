@model IEnumerable<SiteTriks.Models.Comments.CommentViewModel>

@if (Model.Count() > 0)
{
    @foreach (var item in Model.OrderBy(c => c.DateCreated))
    {
        @Html.DisplayFor(c => item)
    }
}
